namespace = demon

# Event to grant influence when a pop is purged
country_event = {
	id = demon.3
	hide_window = yes
	is_triggered_only = yes

	trigger = {
		has_civic = "civic_satanism"
	}

	immediate = {
		add_resource = {
			influence = 0.5
		}
	}
}
